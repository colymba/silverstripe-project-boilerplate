'use strict';

var ssProject = window.ssProject;

(function($){

  ssProject = {
    init: function()
    {
      // called when it is all ready
    }
    // Project's javascript methods and cool stuff here
    // under the ssProject namespace!
  };

  ssProject.init();

})(jQuery);